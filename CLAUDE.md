# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with
code in this repository.

**The full, canonical guidance lives in [AGENTS.md](AGENTS.md).** Read it — it is
the single source of truth for this repo's architecture, the API data layer
(`types → services → hooks`), the auth/role model, commands, and conventions.
This file is intentionally a pointer so the two never drift.

See also: [API_INTEGRATION_NOTES.md](API_INTEGRATION_NOTES.md) for the
per-screen backend coverage map (which screens are wired to the real API vs.
still on mock data) and the `backend-gaps-*.json` files for per-role gaps.
